## Chatbot Agent Requirements
### Objectives
1. Use different message types - HumanMessage and AIMessage
2. Maintain a full conversation history using both message types
3. 
    a. (If you have OPEN_API_KEY) Use GPT-4o model using LangChain's ChatOpenAI
    b. (If you don't have OPEN_API_KEY) Use GPT-4o model using LangChain's ChatOpenAI
4. Create a sophisticated conversation loop

5. ***Main Goal: Create a form of memory for our Agent***